# Required for Allure's JUnit 5 listener (io.qameta.allure.junit5.AllureJunit5)
# to be picked up automatically — it's registered via ServiceLoader in the
# allure-junit5 jar, and JUnit 5 only auto-detects such extensions when this
# is turned on.
junit.jupiter.extensions.autodetection.enabled=true
